"""Shared launch helpers for gazebo_launch.

The launch entrypoints add the package share root to sys.path before importing
this package, so these helpers work both from source and from install space.
"""

